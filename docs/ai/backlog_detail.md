# Vote Detail — Backlog

> `feature/vote-detail` 패킷 이후 다음 단계 제안

## 다음 패킷 후보

### 1. 공유 SDK 실연결
- 현재: 카톡/인스타 버튼은 더미 토스트, URL만 `navigator.clipboard` 호출
- 다음: `@apps-in-toss/web-framework`의 share API로 카카오톡/인스타그램 스토리 채널 실동작
- 의존: AIT 공유 SDK 문서 확인, 권한 정책 점검
- DoD 후보:
  - 카카오톡 버튼 → 토스 share sheet 호출, 미리보기에 질문 텍스트 + 참여자 수만 노출 (결과 블라인드)
  - 인스타 버튼 → 스토리 비율 이미지/링크 공유 트리거
  - SDK 미지원 환경에서는 기존 더미 토스트로 fallback

### 2. 결과 이미지 저장
- 기획: pickit_plan §9 — 인스타 스토리 비율(9:16) 결과 이미지 저장
- 다음: 결과 화면을 `<canvas>` 또는 html-to-image로 캡처, 저장/공유 진입점 제공
- 의존: 캡처 라이브러리 선택 (번들 영향 검토), AIT 파일 저장 API
- DoD 후보:
  - "이미지 저장" 버튼 추가 → 9:16 비율 PNG 생성
  - 저장된 이미지에 질문/전체 결과 바/참여자 수 포함

### 3. 재투표 방지 + 결과 상태 영속화
- 현재: 새로고침/재진입 시 myOptionId 사라져 다시 unvoted로 복귀
- 다음: localStorage 또는 토스 인증 기반으로 `voted:{voteId} = optionId` 저장 → 재진입 시 즉시 result 상태 + 내 선택 강조
- DoD 후보:
  - 재진입 시 이전 선택 복원
  - "이미 투표했어요" 안내 + 결과만 노출

### 4. AIT WebView 뒤로가기 호환성 검증
- 리스크: 토스 네이티브 뒤로가기 제스처가 SPA history와 어떻게 묶이는지 미확인
- 다음: AIT SDK의 navigation/back-event 문서 검토, 필요 시 `useEffect` + back 이벤트 리스너로 동기화
- DoD 후보:
  - 토스 앱 내에서 네이티브 뒤로가기 → `/` 복귀 동작 검증
  - 상세 → 홈 → 상세 순회 시 history 정상

### 5. `ait build` 빌드 파이프라인 점검 (별도 패킷)
- 현재 `package.json` `"build": "ait build"`는 현 ait 버전에서 미존재 명령
- 다음: 올바른 빌드 명령(`granite build` 등) 확인 후 교체, CI에 빌드 검증 단계 포함
- 패킷 1번보다 선행 권장 (배포 가능 상태 확보)

### 6. 신고 버튼 (후순위)
- 기획: pickit_plan §6 3단계 — 부적절한 질문/선택지 신고
- 다음: 결과 화면 헤더 우측에 메뉴 → 신고 진입
- 운영자 검토 워크플로우는 별도 추적

---

## 우선순위 제안

| 순서 | 패킷 | 이유 |
|------|------|------|
| 1 | `ait build` 점검 | 배포 차단 이슈, 가장 먼저 해소 |
| 2 | 재투표 방지 | UX 즉시 체감, 백엔드 의존 적음 |
| 3 | 공유 SDK 실연결 | 바이럴 핵심 경로 활성화 |
| 4 | AIT 뒤로가기 호환성 | 실 디바이스 검증 필요 |
| 5 | 결과 이미지 저장 | 공유 후속 — 이미지 캡처 결정 후 |
| 6 | 신고 버튼 | MVP 이후 운영 단계 |
